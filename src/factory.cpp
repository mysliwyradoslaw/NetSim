#include "factory.hxx"

Factory::remove_receiver(NodeCollection<Node>& collection, ElementID id) {};

bool has_reachable_storehouse(const PackageSender* sender, std::map<const PackageSender*, NodeColor>& node_colors);
